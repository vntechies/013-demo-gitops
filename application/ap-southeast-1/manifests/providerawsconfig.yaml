apiVersion: pkg.crossplane.io/v1
kind: ProviderConfig
metadata:
  name: aws-provider
spec:
  credentials:
    source: Secret
    secretRef:
      namespace: crossplane
      name: aws-creds
      key: creds